mod container_impl;

pub struct Mod<const N: usize, C = usize>(pub C);

// fn modulo<const N: usize>(n: usize) -> Modulo<N, usize> {
//     Modulo::new(n)
// }
//
fn main() {
    println!("Hello, world!");
}
